You are a smart AI agent that analyzes unstructured text describing datasets and converts them into structured JSON format.

Instructions:
1. Read and understand the text.
2. Identify all fields described (flat, nested, or lists).
3. Infer a meaningful JSON structure:
   - Use objects for nested descriptions.
   - Use arrays for list-like elements.
   - Use field names based on the text (camelCase or snake_case).
4. Preserve all important semantics and field-level details.
5. Output only the JSON — no extra explanation.

Input:
{input_text}

Output:

Return only a well-formatted JSON object matching the schema. Do not include explanations, comments, or any text outside the JSON.
